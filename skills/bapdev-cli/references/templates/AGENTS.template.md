# AGENTS.md

本文件为当前云开发工程的 AI 协作指南。

## 项目概述

[待补全]

- 项目名称：根据.bapdev-cli目录下的信息获取项目名称，如果前缀是`gpf_dc_`去除前缀
- 产品：GPF
- JDK 版本：JDK 1.8
- 构建约束：根据当前工程真实信息填写

## 目录结构

[待补全]

请根据当前工程的实际目录结构补全，建议至少覆盖以下类型：

- `src/` 或其下各源码子目录
- `lib/` 依赖目录
- `conf/` 配置目录
- `.bapdev-cli/` 云工程本地配置目录
- `openSource/` 或其他源码参考目录

## 技能入口

[待补全]

本节不要直接罗列 `bapdev-cli` 子命令，而应说明：

- 哪些技能可用于 BAP 云工程操作与代码编译
- 哪些技能可用于代码开发
- 各技能分别适用于什么场景

其中：

- BAP 云工程操作与本地编译，优先归入 `bapdev-cli`
- 代码开发技能应根据当前工程实际可用技能补全

当前仓库已存在的 `bapdev` / `gpf` 相关技能，可按实际工程场景筛选补全：

- `bapdev-cli`
  - 用于 BAP 云工程环境初始化、下载、刷新、更新、提交、发布、依赖同步、获取云端当前代码、本地编译
- `gpf-dev-orchestrator`
  - 用于在多种 GPF 开发任务之间路由和选择合适技能
- `gpf-api-call`
  - 用于编写和核实 GPF 管理器 API、数据对象与平台调用代码
- `gpf-cloud-cell`
  - 用于云开发 Cell、服务 Cell、资源 Cell 等组件开发
- `gpf-http-interface`
  - 用于 HTTP 接口、RequestMapping、分发配置、拦截器相关开发
- `gpf-microservice`
  - 用于 GPF 微服务相关开发与接口组织
- `gpf-permission-matrix`
  - 用于权限矩阵、权限控制规则相关开发
- `gpf-rule-function`
  - 用于规则函数、校验、填值、过滤等规则开发

补全时应根据当前工程真实任务场景选择：

- BAP 云工程操作与本地编译：优先写入 `bapdev-cli`
- 通用 GPF 开发技能：按工程实际涉及的技术形态补充
- 若某技能与当前工程无关，不要硬写入最终 `AGENTS.md`
- SHOULD 只保留当前工程真正会用到的 3 到 6 个技能
- 技能说明 MUST 按“什么任务用什么技能”来写，MUST NOT 写成纯目录清单

## 构建与编译

[待补全]

本节应说明当前工程的本地编译方式、输出目录、`.classpath` 依赖规则，以及是否需要先同步依赖库。

## 测试约定

[待补全]

本节用于说明当前工程的测试方式、测试框架、测试类命名规范和运行入口。如果工程中未发现明确测试入口，应直接写明，不要套用默认测试框架。

## 代码风格

[待补全]

本节用于约定：

- Java 命名规范
- 导入顺序
- 缩进与格式化规则
- 注释规范
- 工程特有的包路径或类命名约束

如果需要查询平台现有源码、接口定义、类签名或实现细节，必须在最终 `AGENTS.md` 中明确写出以下查询顺序：

1. 优先查询 `gpf-api-call` 技能内已收录的源码快照与参考资料
2. 若 `gpf-api-call` 中没有，再查询工程依赖中 `gpf_dc` 前缀 jar 包对应的 Java 源码
3. 若前两者都没有，再查询 `openSource/` 目录下的 `src.zip`

该顺序应作为固定约束写入，避免在缺少依据时直接跳到低优先级源码来源。

建议补充以下 GPF 开发规范：

- 规则函数开发
  - 接口必须继承 `CellIntf`
  - 包名必须以 `cell.` 开头
  - 必须补齐 `@ClassDeclare`、`@MethodDeclare`、`@InputDeclare`
  - 环境变量按约定使用 `exampleValue`

- HTTP 接口开发
  - 接口定义类必须继承 `RequestMappingIntf`
  - 必须补齐 `@ClassDeclare`、`@MethodDeclare`
  - 参数必须在 `@MethodDeclare.inputs` 中声明
  - 路径统一使用 `@RequestMapping`
  - 未明确限定时，`method` 默认同时支持 `GET` 和 `POST`

- 云服务开发
  - 服务类按 Cell 体系开发
  - 服务 Cell 继承服务型基类，资源 Cell 使用资源型基类
  - 生命周期资源必须在启动/停止阶段显式管理
  - 获取方式、线程模型和资源释放规则应与当前工程约定保持一致

## 云工程协作规范

[待补全]

本节用于说明当前工程通过云工程开发时的约束，建议至少包括：

- 先完成工程环境初始化，再执行依赖连接的命令
- 提交前先刷新差异
- 需要时先获取云端当前代码再比较
- 下载后是否建立 Git 基线
- `.bapdev-cli/` 属于本地状态，不应直接提交

## 依赖管理

[待补全]

本节用于说明依赖目录划分、`.classpath` 的作用，以及何时应执行依赖同步命令。

- MUST NOT 在本地工程中自行引入新的 jar 包。
- 如确需新增依赖，MUST 先询问用户，并由用户在云工程中完成依赖添加。
- 完成云工程依赖变更后，MUST 再执行依赖同步命令更新本地 `lib/` 与 `.classpath`。

## 环境要求

[待补全]

本节用于说明当前工程所需的 JDK、IDE、终端环境和其他必要工具。若仓库中无明确依据，不要补写默认版本。

## 注意事项

[待补全]

本节用于汇总当前工程的高风险约束，例如：

- 敏感信息不得写入源码或版本库
- 本地配置文件不应提交
- 发布前必须先完成本地编译或指定检查
- 某些目录仅允许通过云工程命令同步
- MUST NOT 绕过云工程依赖管理机制直接新增本地 jar
