# GPF 权限领域扩展总览

本资料用于补充规则函数场景中的权限领域差异，不单独定义另一套规则函数开发形态。

如果最终产物仍是 `CellIntf` 规则函数接口或实现，应先使用 `gpf-rule-function`，再补充本资料。

## 两阶段模型

### 身份识别

回答“谁具备什么身份”。

### 权限授予

回答“这些身份拥有什么权限”。

## 常见控制对象

- 字段可见
- 字段可写
- 按钮可见
- 按钮可执行

## 常见场景

- 按创建人动态授权
- 按部门、岗位或角色识别身份
- 按关联业务数据控制权限
- 判断某个权限需求是否已经越界成完整矩阵设计
