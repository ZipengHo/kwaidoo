## 前期准备

1. 登录容器需要通过跳板机，登录跳板机后通过 ``ssh root@podman230 -p 容器端口`` 命令跳转到容器，所以要先申请跳板机账号

2. 如果不确定容器的端口可以再[计算资源管理](https://trial.kwaidoo.com/automation/ccrm/)的【申请PAAS环境】中找到之前的申请记录，在【映射配置】的【转发端口】后两位改成22就是ssh的端口，例如HTTP的转发端口为“27009”这ssh端口为“27022”。

![img](assets/wps31-1780363609702-2.jpg)



## 备份

1.在项目的`/conf/starter.xml` 文件中的“DB链接URL”中可以![image-20260601160522496](assets/image-20260601160522496.png)



2.使用 `pg_dump` 工具将目标数据库导出为 SQL 脚本文件。

基础命令格式：

```bash
pg_dump -U [数据库用户名] -h [数据库主机地址] -p [端口] -d [源数据库名] > [导出文件名.sql]
```

实际操作示例： 用户名为 `postgres` 密码为 `123456`，假设要导出的数据库名为 `GPF`，导出的文件命名为 `GPF_backup_test.sql`。

```bash
# 执行导出命令 
pg_dump -h postrges -U postgres -d GPF > GPF_backup_test.sql
```

![image-20260602092823198](assets/image-20260602092823198.png)



3.数据导出完成后，使用 `scp` 命令将生成的 sql 文件安全传输到目标服务器。

基础命令格式：

```bash
scp [本地文件路径] [目标服务器SSH用户名]@[目标服务器IP]:[目标服务器存放路径]
```

**实际操作示例：** 假设目标服务器 IP 为 `14.18.100.250`，SSH 登录用户为 `hezp`，我们要把文件存放到目标服务器的 `/home/hezp` 目录下。

```bash
scp GPF_backup_test.zip hezp@14.18.100.250:/home/hezp
```



## 导入

1. 通过 `psql -h postgres -U postgres` 命令链接数据库，密码默认为 `123456`

2. 使用 `create database 新数据库名` 命令创建一个新的数据库。这里需要注意，在创建数据库时名称没用引号包裹的话数据库名称会变为全小写，可以在创建后通过  `\l` 命令查看确认数据库是否正确创建。

   ![image-20260601175211236](assets/image-20260601175211236.png)

3. 回到备份文件的路径通过命令导入数据库内容

​	基础命令格式：

```bash
psql -h [数据库主机地址] -U [数据库用户名] [目标数据库名] < [SQL文件存放路径]
```

​	实际操作示例： 假设要导入的SQL文件名为 `GPF_backup_test.sql`，导入到名为 `gpf_backup_test` 数据库中。

```bash
 psql -h postgres -U postgres gpf_backup_test < /data/GPF_backup_test.sql
```



1. 修改项目中 `/conf/starter.xml` 文件建配置，其中 “DB链接URL” 一行中修改里面的数据库名称改为新添加的数据库名称

   ![image-20260601175910586](assets/image-20260601175910586.png)

2. 修改完配置后到[计算资源管理](https://trial.kwaidoo.com/automation/ccrm/)的【云服务器】重启对应的服务。

   ![image-20260601175439533](assets/image-20260601175439533.png)

   ![image-20260601175509347](assets/image-20260601175509347.png)

